version: '3.8'
services:
  web:
    image: nginx:alpine
    ports:
      - "${HOST_PORT}:80"
    networks:
      - app-net
  db:
    image: redis:alpine
    networks:
      - app-net

networks:
  app-net:
    driver: bridge
